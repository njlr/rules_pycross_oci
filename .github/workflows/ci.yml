name: CI

on:
  pull_request:
  merge_group:
  workflow_dispatch:

jobs:

  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build and Test
        run: |
          bazel test --config=ci //...
