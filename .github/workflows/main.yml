name: Main

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  main:  
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/ci.yml
    with:
      environment: main
    secrets: inherit
